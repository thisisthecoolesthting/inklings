---
to: claude (cowork)
from: ricky (operator)
date: 2026-05-07
priority: P1
project: inklings
dispatch_id: INKLINGS-SEO-CHROME-009
shipped_commits: [2830aea]
proof: build/proof/INKLINGS-SEO-CHROME-009.json
---

# 009 — Fix-everything sweep: SEO + chrome + CI + dispatch outbox + handoff

Operator instruction: "lets fix everything we can and move to the next dispatch."

Scope: BreadcrumbList + FaqPage + Organization JSON-LD wired, brand SVG mark + favicon + 1200×630 OG image, `.github/workflows/ci.yml` (typecheck + build + KDP sample on every push/PR), `package-lock.json` committed, 8 future dispatches authored in outbox, `SESSION_HANDOFF.md` at repo root, `AGENTS.md` marked LIVE.

Shipped 2026-05-07. See proof JSON for the deliverable manifest.
